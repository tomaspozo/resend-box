# Resend Sandbox Environment Variables
# Copy this file to .env or .env.local and adjust values as needed

# =============================================================================
# Resend SDK Configuration
# =============================================================================
# Base URL for the Resend API mock server
# This is set automatically when you run: resend-sandbox init
# The Resend SDK will use this URL instead of the production API
RESEND_BASE_URL=http://localhost:4657

# =============================================================================
# Sandbox Server Configuration (Optional)
# =============================================================================
# Override the HTTP port for the Resend API mock and Web UI
# Default: 4657
# RESEND_SANDBOX_HTTP_PORT=4657

# Override the SMTP server port
# Default: 1025
# RESEND_SANDBOX_SMTP_PORT=1025

# =============================================================================
# Example: Custom Ports
# =============================================================================
# If you need to use different ports (e.g., if 4657 or 1025 are already in use):
# RESEND_SANDBOX_HTTP_PORT=3000
# RESEND_SANDBOX_SMTP_PORT=2525
# RESEND_BASE_URL=http://localhost:3000
